export enum ERealm {
    asia = 1,
    eu = 2,
    us = 4
}

export enum EGameType {
    solo = 1,
    twosRT = 2,
    twosAT = 4,
    foursRT = 8
}

export enum ERace {
    human = 1,
    orc = 2,
    elf = 4,
    undead = 8,
    random = 16
}